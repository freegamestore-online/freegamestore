name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main]
  # Scheduled rebuild every 6 hours so the per-game "Recent updates"
  # sections on detail pages reflect commits creators push to their
  # OWN repos — without this, the inline 3-commit list stays frozen
  # until the next storefront-repo push.
  schedule:
    - cron: '0 */6 * * *'
  # Manual trigger: lets a creator force an immediate refresh after
  # publishing a hot fix.
  workflow_dispatch:

# Need write access so we can commit the refreshed commit-history cache
# back to the repo. Without this, CF Pages' parallel no-token build
# never sees the fresh data.
permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      # A normal (push) build is OFFLINE: it reads commit history from the
      # committed data/commit-history.json cache and makes ZERO GitHub API
      # calls, so pushes never rate-limit. Only the scheduled / manually
      # dispatched runs set FGS_REFRESH_HISTORY=1 to fetch fresh history from
      # the API (GITHUB_TOKEN lifts the limit 60 → 5000 req/hr) and rewrite the
      # cache, which the "Commit refreshed commit-history cache" step commits.
      - run: node build.js
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FGS_REFRESH_HISTORY: ${{ (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') && '1' || '' }}

      # SDK docs site — Astro Starlight builds into ../dist/docs so the
      # storefront and docs ship in a single wrangler deploy. Lives at
      # freegamestore.online/docs/.
      - name: Build docs site
        working-directory: docs
        run: |
          pnpm install --frozen-lockfile=false
          pnpm build

      # Persist the refreshed history cache so CF Pages' no-token build
      # can read it instead of hitting the rate-limited API. [skip ci]
      # keeps this commit from re-triggering deploy.yml's push handler;
      # the schedule still fires every 6h to refresh.
      #
      # Race-resistance: cron and push triggers can both fire and race
      # on the push. Retry once with a rebase on conflict; accept the
      # loss after two failures since the next cron tick fixes it.
      - name: Commit refreshed commit-history cache
        run: |
          if git diff --quiet data/commit-history.json 2>/dev/null; then
            echo "No history changes to commit."
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/commit-history.json
          git commit -m "Refresh commit-history cache [skip ci]"
          for attempt in 1 2; do
            if git push; then
              echo "Pushed cache on attempt $attempt."
              exit 0
            fi
            echo "Push attempt $attempt failed — rebasing on remote and retrying."
            git fetch origin main
            git reset --soft HEAD~1
            git stash --include-untracked --quiet
            git rebase origin/main
            git stash pop --quiet
            git add data/commit-history.json
            git commit -m "Refresh commit-history cache [skip ci]" --allow-empty
          done
          echo "Two push attempts failed; leaving cache uncommitted."

      - run: npx wrangler pages deploy dist --project-name=freegamestore
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: c1089bfcc43c1c6c2aa89e584e86f0bc
